#include<stdio.h>
int main()
{
    int n = 9;
    printFib(n);
    return 0;
}
else {
        if (b >= c)
            printf("%d is the largest number.", b);
        else
		printf("%d is the largest number.", c);
    }

    return 0;
}
